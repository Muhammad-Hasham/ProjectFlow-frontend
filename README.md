# ProjectFlow-frontend

## Setup Instructions

1. Open the `ProjectFlow-frontend` folder in VS Code.
2. Open a terminal in VS Code.

## Installation

Run the following commands to install the required packages and run the application:

```sh
npm install --force
npm start